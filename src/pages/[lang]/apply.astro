---
import Layout from "../../layouts/Layout.astro"
import { getLangFromUrl, useTranslations } from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

if (!["es", "en"].includes(Astro.url.pathname.split("/")[1])) {
  return new Response(null, { status: 404 });
}
---

<Layout title={t("apply.title")} image="/pages/staff.webp" desc={t("apply.desc")}>
    <iframe id="" allowtransparency="true" allowfullscreen="true" allow="geolocation; microphone; camera" src="https://3a6m514f.forms.app/form/67bd34a11a3737e353d2626a" frameborder="0" class="w-full h-[calc(100vh-55px)]"></iframe>
</Layout>